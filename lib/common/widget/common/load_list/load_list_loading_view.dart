import 'package:flutter/material.dart';

class LoadListLoadingView extends StatelessWidget {
  const LoadListLoadingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: SizedBox(),
    );
  }
}
